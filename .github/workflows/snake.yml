name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"   # runs daily at 12 AM UTC
  workflow_dispatch:     # allows manual run

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Generate GitHub Contribution Snake
        uses: Platane/snk@v3
        with:
          github_user_name: Tarun0711
          outputs: |
            dist/github-contribution-grid-snake.name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"   # runs daily at 12 AM UTC
  workflow_dispatch:     # allows manual run

permissions:
  contents: write   # 🔴 THIS IS THE KEY FIX

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Generate GitHub Contribution Snake
        uses: Platane/snk@v3
        with:
          github_user_name: Tarun0711
          outputs: |
            dist/github-contribution-grid-snake.svg

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
svg

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
